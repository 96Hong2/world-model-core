// 도메인 프로필. 화면에 박히는 제품·모델 이름과 문구를 여기서만 정한다.
//
// 이 저장소의 기본값은 가상 도메인(오로라소프트가 협업 솔루션 오로라웍스를 파는
// 세계)이다. 자기 도메인에 쓰려면 이 파일과 config/*.yaml 을 바꾸면 된다.
// 코드 어디에도 제품 이름을 직접 쓰지 않는다.

export const PROFILE = {
  /** 이 월드모델이 다루는 제품·사업의 이름 */
  productName: "오로라웍스",
  /** 화면 곳곳에 쓰는 시스템 이름 */
  modelName: "오로라웍스 World Model",
  /** 좌측 레일 마크 같은 좁은 자리에 쓰는 약칭 */
  modelAbbr: "WM",
} as const;
